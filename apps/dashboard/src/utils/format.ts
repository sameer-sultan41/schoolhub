// Placeholder — no shared date/currency/number formatting helper exists yet (the two
// Metronic demo widgets that format inline are flagged as demo content, not a real need).
// Add real formatters here when a real screen needs one.
export {};
