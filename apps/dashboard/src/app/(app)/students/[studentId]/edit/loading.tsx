import { FormRouteLoading } from "@/components/route-loading";

/** The same fourteen fields as the create form, since it is the same form. */
export default function Loading() {
  return <FormRouteLoading fields={14} />;
}
