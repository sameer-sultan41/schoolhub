import { TableRouteLoading } from "@/components/route-loading";

/** The roster table: a checkbox column, admission number, name and status. */
export default function Loading() {
  return <TableRouteLoading columns={4} />;
}
