# Project Status — schoolhub-frontend

Living hand-off note: what exists, what is deliberately missing, and where the next session
should start. **Update this file in the same PR as the work it describes.**

Spec repo (source of truth): <https://github.com/sameer-sultan41/schoolhub-srd>
Read [`../AGENTS.md`](../AGENTS.md) first — especially the context-map rule.

---

## Done — Scaffold (initial commit)

The monorepo skeleton is in place and structurally complete. Nothing has been installed,
built, or run locally: **CI is the source of truth** for lint/typecheck/test/build.

| Area | State |
| ---- | ----- |
| Workspace root | pnpm workspace, Turborepo (`dev/build/lint/test/test:coverage/typecheck/clean`), `tsconfig.base.json` (strict + `noUncheckedIndexedAccess`), `.npmrc`, `.nvmrc` (Node 24), `.env.example`, `.gitignore` |
| CI | `.github/workflows/ci.yml` — install → lint → typecheck → test, then a build matrix over `dashboard`/`website`, plus a gitleaks secret scan |
| `packages/types` | API envelope `{data, meta}` / `{error:{code,message,details,request_id}}`, cursor + offset pagination, job resource, auth/RBAC types (`PermissionKey = module.resource.action`), tenant + branding, website CMS content types |
| `packages/api-client` | Hand-written transport core: envelope unwrapping, bearer auth, **single-flight refresh-on-401 + replay**, error normalization into `ApiError` (incl. `network_error`), cursor pagination helpers (`paginate`, `collectPages`, `getNextPageParam`), in-memory access-token store. Jest tests cover all of it. README states the resource layer is REGENERATED from the OpenAPI spec |
| `packages/ui` | `Button`, `Card`, `DataTable` (cursor controls, loading/empty states), `FormField`/`Input`, `cn`, `brandingToCssVariables` (sanitized), and `src/styles/theme.css` — the Tailwind v4 `@theme inline` token layer driven by `--sh-*` variables |
| `packages/config` | Shared ESLint flat config (ESLint 10, typescript-eslint) |
| `apps/dashboard` | Auth-guard middleware, in-memory access token + refresh wiring, validated env, TanStack Query client + key factory, `hasPermission`/`<Can>`, `(auth)/login` with RHF + Zod, `(app)` shell with permission-filtered nav + tenant theming, dashboard page with permission-gated tiles, `/api/health`, next-intl (`en` + `ur`, RTL) |
| `apps/website` | Host→tenant middleware (spoofed header stripping), cached tenant resolution, **read-only** content layer, `[...slug]` + `/` ISR rendering of `website_pages`/`page_sections`, theme registry + 12 section components + navigation/footer, per-tenant `sitemap.ts`/`robots.ts`, HMAC-signed revalidate webhook, unknown-host platform page |

Both apps were generated with `create-next-app` (Next 16, Turbopack, Tailwind 4, `src/`,
`@/*`) and then customized; the Next-managed block at the top of each app `AGENTS.md` is
regenerated by `next dev` and must stay.

---

## Deliberately NOT done

- **No `pnpm install` / lockfile.** First CI run (or the first local install) creates
  `pnpm-lock.yaml`. Until then no version has been resolved — see the risk list below.
- **No E2E layer.** Jest only, no Playwright (team decision; the spec doc names Vitest +
  Playwright — this repo uses Jest and no E2E).
- **No generated API resource layer.** `packages/api-client` ships the transport core only;
  `students.list()`-style resources arrive with the OpenAPI generator.
- **No module screens.** Only the dashboard home exists. Students, fees, attendance, … are
  untouched.
- **No shadcn/ui CLI install.** `packages/ui` hand-implements the few components needed so far
  in the shadcn idiom (cva + tokens), with no Radix dependency yet.

---

## Start here next session

1. **Get CI green.** Push, watch `gh pr checks`, and fix what the first real install/typecheck
   surfaces. Highest-risk pins to confirm: `typescript@^7`, `eslint@^10` with
   `typescript-eslint@^8`, `@eslint/js@^10`, `jest@^30` under `next/jest`, and
   `next-intl@^4` against Next 16.
2. **Commit the lockfile** that install produces (CI uses `--frozen-lockfile`).
3. **Decide the typed-routes question.** App code uses explicit prop types rather than Next 16's
   generated `PageProps`/`LayoutProps` globals, so `tsc --noEmit` works without a prior build.
   If the team prefers the generated globals, add `next typegen` to the `typecheck` script.
4. **Wire the OpenAPI generator** (`pnpm generate:api`) once the backend publishes its spec, and
   delete any hand-written resource shapes it replaces.
5. **Then build the first module screen** end-to-end (students list is the natural first) using
   `queryKeys.list()`, `DataTable`, and `<Can>` — it will prove the whole stack.

## Conventions worth re-reading before writing code

- Tenant branding is the only source of brand colour (`--sh-*` custom properties). No literal
  hex, no `blue-600`.
- Permission-aware UI is UX, never enforcement.
- `apps/website` must never gain a write path.
- Every new env var goes into `.env.example` with a dummy value and a comment.
